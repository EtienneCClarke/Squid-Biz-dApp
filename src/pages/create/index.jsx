import { SideBar } from "../../components";

export default function Create() {

    return(
        <main>
            <SideBar />
            <div className="view">
                
            </div>
        </main>
    );
}